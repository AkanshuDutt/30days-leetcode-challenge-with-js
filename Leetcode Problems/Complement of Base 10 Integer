
/**
 * @param {number} n
 * @return {number}
 */
var bitwiseComplement = function(n) {
      if (n === 0) 
        return 1;

    let mask = 0;
    let x = n;
    while (x) {
        mask = (mask << 1) | 1;
        x >>= 1;
    }
    return mask ^ n;
};
